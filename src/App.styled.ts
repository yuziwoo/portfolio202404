import styled from 'styled-components';

const App = styled.div`
  width: 100%;
  overflow: hidden;
`;

const Main = styled.main`
  width: 100%;
  position: relative;
`;

export const AppStyle = {
  App,
  Main
};
