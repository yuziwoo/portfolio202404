import { elementId } from '../../../constants/elementId';
import { ComponentStyle as S } from './Header.styled';

const Header = () => {
  return <S.Header id={elementId.layout.HEADER}></S.Header>;
};

export default Header;
